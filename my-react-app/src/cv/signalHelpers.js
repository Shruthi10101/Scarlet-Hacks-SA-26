export function toActivityScore(movementScore) {
  return Math.round(movementScore * 100);
}

export function toRestlessnessScore(restlessnessScore) {
  return Math.round(restlessnessScore * 100);
}

export function toFeedingEngagementScore(feedingScore) {
  return Math.round(feedingScore * 100);
}